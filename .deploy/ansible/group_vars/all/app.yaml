---
work_dir: "/opt/google-auth"

application:
  name: google-auth
  port:
    tcp:
      name: tcp
      number: 8000
    actuator:
      name: actuator
      number: 7000
  path:
    config: "{{ work_dir }}/config"
    logs: "{{ work_dir }}/logs"
  version: "{{ lookup('env', 'DOCKER_VERSION') }}"