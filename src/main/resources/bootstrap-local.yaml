spring:
  application:
    name: google-auth
  cloud:
    kubernetes:
      enabled: false

server:
  port: ${APP_PORT}

management:
  server:
    port: ${MANAGEMENT_PORT}
  endpoints:
    web:
      exposure:
        include:
          - health
          - prometheus