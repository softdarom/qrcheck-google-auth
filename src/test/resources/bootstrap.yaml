spring:
  application:
    name: google-auth
  cloud:
    kubernetes:
      enabled: false

server:
  port: 8888

management:
  server:
    port: 7777
  endpoints:
    web:
      exposure:
        include:
          - health
          - prometheus